---
"@tisyn/kernel": minor
---

Add `ProhibitedEffectError` for scoped effects.

- Add `ProhibitedEffectError` thrown when an IR middleware expression attempts to use any effect other than `dispatch`
