---
"@tisyn/ir": minor
---

Tighten `IrInput` and `Call()` typing, align `Eval()` with a single-payload canonical
form, and improve `print()` so generated constructor output remains valid and
round-trippable.
