---
"@tisyn/example-multi-agent-chat": minor
---

Merge descriptor.ts into workflow.ts so the demo runs directly from TypeScript source via `tsn run src/workflow.ts -e dev`. Local and inprocess transport refs now point to `.ts` source modules instead of compiled `.js`.
