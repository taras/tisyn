---
"@tisyn/runtime": patch
---

Add tests for built-in sleep effect: compiled sleep succeeds, replay works, dispatch middleware intercepts, unknown effects still fail, `Effects.sleep(ms)` works directly.
