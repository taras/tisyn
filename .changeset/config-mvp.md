---
"@tisyn/config": minor
---

Add `@tisyn/config` package — pure descriptor constructors (`workflow`, `agent`, `transport`, `env`, `journal`, `entrypoint`, `server`), validation rules V1–V10 with single-pass recursive walk, and `walkConfig`/`collectEnvNodes` tree traversal helpers.
