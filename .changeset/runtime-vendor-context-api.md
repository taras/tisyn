---
"@tisyn/runtime": patch
---

Swap the preview `@effectionx/context-api` dependency for
the in-repo workspace vendor `@tisyn/context-api`. No
behavior change in public `@tisyn/runtime` API or
observable Runtime-context / middleware-composition /
replay semantics.
